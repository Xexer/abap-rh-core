projection;
strict ( 2 );
use draft;
use side effects;

define behavior for ZRH_C_ContactTP alias Contact
{
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

  use action createAddress;
  use action createCustomer;
  use action createEmployee;
}